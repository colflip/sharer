{
    "name": "screencast-static",
    "version": "0.1.0",
    "private": true,
    "description": "Static Agora-based screen sharing pages.",
    "scripts": {
        "serve": "python3 -m http.server 4173",
        "check": "node --check assets/js/config.js && node --check assets/js/agora-loader.js && node --check assets/js/viewer.js && node --check assets/js/sharer.js"
    }
}
